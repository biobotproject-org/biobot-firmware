# Archive

Superseded code kept for reference. Nothing here is built or maintained.

- `new.grc/` — the first prototype sketch (early 2026). Talked to the
  Notecard over UART, read an analog soil sensor, and used a different
  BMV080 driver. Replaced by `Firmware/Notecard/`.
